---
phase: v5.5.0-stack-customization
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - skills/init-project/SKILL.md
  - commands/add-tech-stack.md
  - commands/sync-stack-skill.md
autonomous: true
requirements: [STACK-01, STACK-06]

must_haves:
  truths:
    - "skills/init-project/SKILL.md has new T1 architecture detection sub-flow + creates registry.json on init"
    - "commands/add-tech-stack.md and commands/sync-stack-skill.md exist as Markdown slash command registrations consumable by Claude Code + Cursor"
  artifacts:
    - "skills/init-project/SKILL.md (with T1 sub-flow + registry init)"
    - "commands/add-tech-stack.md"
    - "commands/sync-stack-skill.md"
  key_links:
    - "init-project section f file creation list now includes registry.json from template (consumed: registry.json template from Plan 01)"
    - "T1 sub-flow inserted between section d (Brownfield Handling) and section e (Interactive Prompts)"
    - "slash commands match v5.4.0 commands/init-project.md + commands/update-config.md format pattern"
---

<!--
  WAVE 3 — Update init-project + create slash commands. 3 tasks independent file edits, parallel-safe.
  Spec reference: docs/specs/2026-05-07-stack-customization-v5.5.0-design.md
-->

<wave number="3" description="Update init-project + create slash command registrations (parallel)">

  <task type="auto">
    <name>Update skills/init-project/SKILL.md to add T1 architecture detection + registry.json creation</name>

    <model>sonnet</model>

    <read_first>
      skills/init-project/SKILL.md
      docs/specs/2026-05-07-stack-customization-v5.5.0-design.md
      docs/claude/templates/registry.json
    </read_first>

    <files>
      <modify>skills/init-project/SKILL.md</modify>
    </files>

    <action>
      Modify `skills/init-project/SKILL.md` in two places:

      **Change 1 — Section f (File Creation):** add registry.json copy step after the existing template copy steps. Locate the line `- `settings.json` → `.claude/settings.json` — copy as-is.` and APPEND a new step:

      ```markdown
         - `registry.json` → `.claude/stack-skills/registry.json` — fill `{PLUGIN_VERSION}` (read from `.claude-plugin/plugin.json` `version` field at runtime) and `{INIT_DATE}` (today's date).
      ```

      Also update step 1 (mkdir) to include the new directory:
      ```markdown
      1. `mkdir .planning/`, `mkdir .planning/research/`, `mkdir docs/specs/`, `mkdir .claude/stack-skills/` (no-op if present). Then create `.planning/research/.gitkeep` and `docs/specs/.gitkeep` (empty files — forces git to track these dirs; `.gitkeep` is a convention).
      ```

      **Change 2 — New Section d2 (T1 Architecture Detection sub-flow):** insert a new section between current section `d` (Brownfield Handling) and section `e` (Interactive Prompts). Header: `## d2. Architecture Detection (T1 trigger)`.

      Content per spec §7.4:

      ```markdown
      ## d2. Architecture Detection (T1 trigger)

      Runs only if section d found brownfield markers (`{DETECTED_STACKS}` non-empty). E3 conservative — every step requires explicit consent.

      1. Display: `❓ Scan architecture for detected stack(s) too? [y/n]`. Default `n` on enter.
      2. If user answers `n`: skip entire section d2. Proceed to section e. Architecture sections in stack skills will use plugin defaults.
      3. If user answers `y`: for each stack in `{DETECTED_STACKS}`:
         a. Display: `── Architecture detection for '<stack>' ──`
         b. Prompt: `❓ Use web search to verify against current best practice? [y/n]`. Default `n`.
         c. Run heuristic detection:
            - Glob folder structure relative to project root.
            - Read 2-3 representative files (e.g., for `dotnet`: `Program.cs`, `*.csproj`, `Startup.cs`; for `react`: `package.json`, `src/App.tsx`).
            - Match against known patterns: Clean Architecture (Domain/Application/Infrastructure layout), Vertical Slices (Features/<feature>/), N-Tier (Controllers/Services/Repositories/), Feature-folders (features/<name>/), Atomic Design (atoms/molecules/organisms/).
            - Compute confidence: HIGH (3+ markers), MEDIUM (2 markers), LOW (<2 markers — fall back to "unknown").
         d. If web search opted in: WebSearch best-practice patterns for `<stack>` stack at current versions; cross-reference with heuristic detection.
         e. Display: `Detected pattern: <pattern> (confidence: <level>)`. Show evidence list (folders found, files inspected).
         f. Prompt: `❓ Save this detection to .claude/stack-skills/<stack>/SKILL.md ## Architecture section? [y / edit / skip]`.
            - `y`: create snapshot directory `.claude/stack-skills/<stack>/`, copy plugin default `{PLUGIN_PATH}/skills/implementer-<stack>/SKILL.md` to `.claude/stack-skills/<stack>/SKILL.md`, replace `## Architecture` section content with detected pattern + evidence summary. Update registry: `source` flips `plugin` → `customized`.
            - `edit`: prompt user to provide content (multi-line); same write flow.
            - `skip`: do nothing, plugin default remains in effect.
      4. After all detected stacks processed, regenerate CLAUDE.md `<!-- stack-table -->` block from registry (sources may have flipped).

      **Greenfield (`{DETECTED_STACKS}` empty)**: section d2 is skipped entirely. No prompt.

      **Edge cases (per spec §8.6):**
      - 0 patterns matched: `❓ No architecture markers found. Skip detection? [y/manual-input]`.
      - Conflicting patterns: `❓ Multiple patterns detected: [<list>]. Pick primary or describe own:`.
      - Web search fail: fallback to heuristic-only with warning.
      - Confidence LOW: show + warn; always prompt user explicitly.
      ```

      AVOID:
      - Don't auto-save detection without user confirmation (E3 conservative).
      - Don't skip section c (idempotency gate) — d2 runs after c if applicable.
      - Don't add `--use-web` flag — web search is always opt-in via prompt.
      - Don't break existing section labeling — d2 is a NEW section between d and e, not a rename of existing sections.

      Corresponds to REQ-IDs: STACK-01 (architecture detection), STACK-06 (registry.json init).
    </action>

    <verify>
      <automated>powershell -Command "$c = Get-Content e:/firstwordflow/skills/init-project/SKILL.md -Raw; $checks = @('## d2. Architecture Detection (T1 trigger)', '.claude/stack-skills/', 'registry.json', '{PLUGIN_VERSION}', '{INIT_DATE}', 'Use web search to verify against current best practice', 'confidence: <level>', 'Greenfield', 'mkdir .claude/stack-skills/'); $missing = $checks | Where-Object { $c -notmatch [regex]::Escape($_) }; if ($missing.Count -eq 0) { Write-Output 'PASS' } else { Write-Output ('FAIL missing: ' + ($missing -join ',')); exit 1 }"</automated>
      <!-- Verifies new section d2 + registry.json copy step + mkdir update + web search opt-in + greenfield handling. <60s. -->
    </verify>

    <done>
      File `skills/init-project/SKILL.md` modified. Has new `## d2. Architecture Detection (T1 trigger)` section between d and e. Section f (File Creation) step 1 includes `mkdir .claude/stack-skills/`. Section f template copy list includes `registry.json` → `.claude/stack-skills/registry.json` with `{PLUGIN_VERSION}` + `{INIT_DATE}` placeholder substitution. Edge cases for 0/conflicting/LOW confidence/web-fail documented. Greenfield path explicitly skips d2.
    </done>

    <stack>plugin-meta</stack>
  </task>

  <task type="auto">
    <name>Create commands/add-tech-stack.md (slash command registration)</name>

    <model>haiku</model>

    <read_first>
      commands/init-project.md
      commands/update-config.md
      skills/add-tech-stack/SKILL.md
    </read_first>

    <files>
      <create>commands/add-tech-stack.md</create>
    </files>

    <action>
      Create `commands/add-tech-stack.md` matching the v5.4.0 pattern from `commands/init-project.md` and `commands/update-config.md`. Read those two files first to copy the format exactly.

      Required content (frontmatter + body invoking the skill):

      ```markdown
      ---
      description: Customize an existing stack skill for current project, or add a new stack not shipped with plugin
      ---

      Run the `add-tech-stack` skill at `skills/add-tech-stack/SKILL.md`.

      Pass through any arguments (stack name, `--force` flag) verbatim.
      ```

      The exact wording inside the body should mirror whatever format `commands/init-project.md` uses (the pattern existed before this plan was written — match it).

      AVOID:
      - Don't add `tags`, `argument-hint`, or other frontmatter keys not used by `commands/init-project.md`.
      - Don't duplicate skill content here — this file just registers the slash command and routes to the skill.
      - Don't change the description style — keep it concise like existing commands.

      Corresponds to: cross-harness slash command registration for `/add-tech-stack`.
    </action>

    <verify>
      <automated>powershell -Command "$c = Get-Content e:/firstwordflow/commands/add-tech-stack.md -Raw; if ($c -match '---' -and $c -match 'description:' -and $c -match 'add-tech-stack' -and $c -match 'skill') { Write-Output 'PASS' } else { Write-Output 'FAIL'; exit 1 }"</automated>
      <!-- Verifies frontmatter present + description + references skill. <60s. -->
    </verify>

    <done>
      File `commands/add-tech-stack.md` exists. Has frontmatter with `description` matching the skill's purpose. Body invokes the `add-tech-stack` skill. Format matches `commands/init-project.md` and `commands/update-config.md`.
    </done>

    <stack>plugin-meta</stack>
  </task>

  <task type="auto">
    <name>Create commands/sync-stack-skill.md (slash command registration)</name>

    <model>haiku</model>

    <read_first>
      commands/init-project.md
      commands/update-config.md
      skills/sync-stack-skill/SKILL.md
    </read_first>

    <files>
      <create>commands/sync-stack-skill.md</create>
    </files>

    <action>
      Create `commands/sync-stack-skill.md` matching the v5.4.0 command pattern. Read `commands/init-project.md` first to copy format.

      Required content:

      ```markdown
      ---
      description: Three-way merge a customized stack snapshot with the latest plugin default
      ---

      Run the `sync-stack-skill` skill at `skills/sync-stack-skill/SKILL.md`.

      Pass through arguments (stack name, or `--all`) verbatim.
      ```

      Match `commands/init-project.md` formatting exactly (frontmatter style, body wording).

      AVOID:
      - Don't add stack-specific arguments to frontmatter — pass through verbatim.
      - Don't restate skill behavior — file is registration only.
      - Don't change the file naming convention — `commands/<command-name>.md` matches the v5.4.0 pattern.

      Corresponds to: cross-harness slash command registration for `/sync-stack-skill`.
    </action>

    <verify>
      <automated>powershell -Command "$c = Get-Content e:/firstwordflow/commands/sync-stack-skill.md -Raw; if ($c -match '---' -and $c -match 'description:' -and $c -match 'sync-stack-skill' -and $c -match 'skill') { Write-Output 'PASS' } else { Write-Output 'FAIL'; exit 1 }"</automated>
      <!-- Verifies frontmatter + description + skill reference. <60s. -->
    </verify>

    <done>
      File `commands/sync-stack-skill.md` exists. Has frontmatter with `description` describing 3-way merge. Body invokes the `sync-stack-skill` skill. Format matches existing commands.
    </done>

    <stack>plugin-meta</stack>
  </task>

</wave>

<!--
  PLAN CHECKER mental review:
  1. Requirement Coverage — STACK-01 (T1 detection sub-flow), STACK-06 (registry.json init from template). ✓
  2. Task Completeness — model + read_first + action + verify + done all present. ✓
  3. Dependency Correctness — depends_on [01, 02] (consumes registry.json template + skills). All 3 tasks edit different files (init-project SKILL, command MD x2). ✓
  4. Key Links Planned — d2 section position (between d and e), registry.json copy step, slash command file format pattern. ✓
  5. Scope Sanity — 3 tasks/plan. ✓
  6. Verification Derivation — must_haves trace to phase truths via verify regex. ✓
  7. Context Compliance — N/A. ✓
  8. Nyquist Compliance — 3 verify commands <60s. ✓
  9. Cross-Plan Data Contracts — init-project section f references registry.json template (Plan 01); slash commands reference skills (Plan 02). ✓
  10. CLAUDE.md Compliance — XML format. ✓
  11. Research Resolution — N/A. ✓
-->
