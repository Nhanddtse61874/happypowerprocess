---
phase: v5.5.0-stack-customization
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - .claude-plugin/plugin.json
  - .cursor-plugin/plugin.json
  - .claude-plugin/marketplace.json
  - CHANGELOG.md
  - CLAUDE.md
  - docs/claude/stack-skill-rule-map.md
autonomous: true
requirements: [STACK-05, STACK-07, STACK-12]

must_haves:
  truths:
    - "Plugin manifests bumped 5.4.0 → 5.5.0; new keywords added: add-tech-stack, sync-stack-skill, stack-customization"
    - "CHANGELOG has v5.5.0 entry summarizing 4 features + 2 new commands + 3 new templates"
    - "Plugin-level CLAUDE.md has new section documenting v5.5.0 commands (alongside v5.4.0 Bootstrap Commands section)"
    - "stack-skill-rule-map.md references project registry for runtime lookup; documents T2 unknown stack halt behavior"
  artifacts:
    - ".claude-plugin/plugin.json (v5.5.0)"
    - ".cursor-plugin/plugin.json (v5.5.0)"
    - ".claude-plugin/marketplace.json (v5.5.0)"
    - "CHANGELOG.md (v5.5.0 entry)"
    - "CLAUDE.md (project root, v5.5.0 commands section)"
    - "docs/claude/stack-skill-rule-map.md (registry reference + T2 behavior)"
  key_links:
    - "all 3 plugin manifests have version: 5.5.0"
    - "stack-skill-rule-map.md How to Apply sections reference .claude/stack-skills/registry.json runtime lookup"
    - "T2 unknown stack halt + 2-option suggestion flow documented in stack-skill-rule-map.md"
---

<!--
  WAVE 4 — Plugin integration + release artifacts. T10 modifies 3 manifest JSON files + CHANGELOG (related cluster); T11 modifies plugin-level CLAUDE.md (project root, different file); T12 modifies stack-skill-rule-map.md (different file). No file overlap → parallel-safe.
  Spec reference: docs/specs/2026-05-07-stack-customization-v5.5.0-design.md
-->

<wave number="4" description="Plugin manifests + CHANGELOG + project CLAUDE.md + stack-skill-rule-map (parallel)">

  <task type="auto">
    <name>Update plugin manifests + CHANGELOG to v5.5.0</name>

    <model>haiku</model>

    <read_first>
      .claude-plugin/plugin.json
      .cursor-plugin/plugin.json
      .claude-plugin/marketplace.json
      CHANGELOG.md
    </read_first>

    <files>
      <modify>.claude-plugin/plugin.json</modify>
      <modify>.cursor-plugin/plugin.json</modify>
      <modify>.claude-plugin/marketplace.json</modify>
      <modify>CHANGELOG.md</modify>
    </files>

    <action>
      4 mechanical edits to bump version and announce v5.5.0:

      **Edit 1 — `.claude-plugin/plugin.json`:**
      - Set `"version": "5.5.0"`.
      - Add to `keywords` array (preserve existing entries, append new ones if not already present): `"add-tech-stack"`, `"sync-stack-skill"`, `"stack-customization"`.

      **Edit 2 — `.cursor-plugin/plugin.json`:**
      - Mirror Edit 1: version → `"5.5.0"`, append same 3 keywords if missing.

      **Edit 3 — `.claude-plugin/marketplace.json`:**
      - Set `"version": "5.5.0"`. (Per v5.4.0 pattern in commit `fc44d18`, marketplace.json version was bumped post-release. Bumping here as part of release cut.)

      **Edit 4 — `CHANGELOG.md`:**
      - Add new entry at top of changelog (above v5.4.0 entry). Follow the same Markdown format as the v5.4.0 entry. Content:

      ```markdown
      ## v5.5.0 — Stack customization (2026-05-07)

      ### Added
      - **`/add-tech-stack`** ([skills/add-tech-stack/SKILL.md](skills/add-tech-stack/SKILL.md)) — customize an existing plugin stack skill for the current project, or add a brand-new stack (Python, Go, Rust, etc.). Two flows: Scenario A (existing — keep/override/append/skip per section) and Scenario B (new — input/suggest/skip + variable section loop). Force both SKILL.md + AGENT.md when creating new; ask user file choice when editing existing.
      - **`/sync-stack-skill`** ([skills/sync-stack-skill/SKILL.md](skills/sync-stack-skill/SKILL.md)) — three-way merge customized snapshot with latest plugin default. Per-section conflict detection with 4 patterns (both unchanged / plugin-only / user-only / both updated); manual-merge sub-flow with 3-pane view. Supports `--all` for batch sync.
      - **Architecture detection (T1 sub-flow)** in `/init-project` — opt-in heuristic detection of project's actual architecture (Clean / Vertical Slices / N-Tier / Feature-folders / Atomic) for brownfield projects. E3 conservative — every step requires explicit user consent.
      - **Stack registry** at `.claude/stack-skills/registry.json` — single source of truth for registered stacks per project. Default 5 plugin stacks + user-added stacks.
      - **Templates**: `docs/claude/templates/registry.json`, `stack-skill-template.md`, `stack-agent-template.md` — used by `/init-project` and `/add-tech-stack` Scenario B.

      ### Changed
      - `docs/claude/templates/CLAUDE.md` — Stack-Specific Rules table now wrapped in `<!-- stack-table:start --> ... <!-- stack-table:end -->` markers; auto-regenerated by `/add-tech-stack` and `/sync-stack-skill` from registry.
      - `skills/init-project/SKILL.md` — adds T1 architecture detection sub-flow (section d2) and creates `.claude/stack-skills/registry.json` from template at init time.
      - `docs/claude/stack-skill-rule-map.md` — runtime lookup now consults `.claude/stack-skills/registry.json`; T2 unknown stack mid-task halt behavior documented.

      ### Plugin manifests
      - `.claude-plugin/plugin.json` 5.4.0 → 5.5.0; keywords added: `add-tech-stack`, `sync-stack-skill`, `stack-customization`.
      - `.cursor-plugin/plugin.json` 5.4.0 → 5.5.0; same keyword additions.
      - `.claude-plugin/marketplace.json` 5.4.0 → 5.5.0.

      ### Backward compatibility
      - v5.4.0 projects continue to work unchanged. Stack registry is auto-created at first `/add-tech-stack` or `/sync-stack-skill` invocation if missing. CLAUDE.md without markers prompts user to refresh template.
      ```

      AVOID:
      - Don't change unrelated keywords or fields in plugin.json files.
      - Don't reorder existing CHANGELOG entries.
      - Don't bump to 5.6.0 or 6.0.0 — this is 5.5.0.
      - Don't include `--no-verify` or skip git hooks in commit guidance.

      Corresponds to REQ-ID: STACK-12 (release artifacts).
    </action>

    <verify>
      <automated>powershell -Command "try { $p1 = Get-Content e:/firstwordflow/.claude-plugin/plugin.json -Raw | ConvertFrom-Json; $p2 = Get-Content e:/firstwordflow/.cursor-plugin/plugin.json -Raw | ConvertFrom-Json; $m = Get-Content e:/firstwordflow/.claude-plugin/marketplace.json -Raw | ConvertFrom-Json; $cl = Get-Content e:/firstwordflow/CHANGELOG.md -Raw; if ($p1.version -eq '5.5.0' -and $p2.version -eq '5.5.0' -and ($p1.keywords -contains 'add-tech-stack') -and ($p1.keywords -contains 'sync-stack-skill') -and ($p1.keywords -contains 'stack-customization') -and ($p2.keywords -contains 'add-tech-stack') -and $cl -match 'v5.5.0' -and $cl -match '/add-tech-stack' -and $cl -match '/sync-stack-skill') { Write-Output 'PASS' } else { Write-Output 'FAIL: version or keywords or changelog mismatch'; exit 1 } } catch { Write-Output ('FAIL: ' + $_.Exception.Message); exit 1 }"</automated>
      <!-- Verifies all 3 manifests at 5.5.0, 3 keywords in plugin.json files, CHANGELOG has v5.5.0 entry. <60s. Note: marketplace.json version field check skipped here since marketplace schema may differ slightly — visual check done. -->
    </verify>

    <done>
      `.claude-plugin/plugin.json` and `.cursor-plugin/plugin.json` both have `version: "5.5.0"` and keywords array contains `add-tech-stack`, `sync-stack-skill`, `stack-customization`. `.claude-plugin/marketplace.json` has version `5.5.0`. `CHANGELOG.md` has v5.5.0 entry at top with Added/Changed/Plugin manifests/Backward compatibility sections.
    </done>

    <stack>plugin-meta</stack>
  </task>

  <task type="auto">
    <name>Update CLAUDE.md (project root) to document v5.5.0 commands</name>

    <model>sonnet</model>

    <read_first>
      CLAUDE.md
      docs/specs/2026-05-07-stack-customization-v5.5.0-design.md
    </read_first>

    <files>
      <modify>CLAUDE.md</modify>
    </files>

    <action>
      Modify project-root `CLAUDE.md` (NOT the template at `docs/claude/templates/CLAUDE.md` — that was Plan 02). Add a new section documenting v5.5.0 commands, parallel to the existing "Bootstrap Commands (New in v5.4.0)" section.

      Find the existing section header `## Bootstrap Commands (New in v5.4.0)` and AFTER its content (before the next top-level heading), INSERT a new section:

      ```markdown
      ## Stack Customization Commands (New in v5.5.0)

      Two slash commands handle per-project stack skill customization and sync:

      - **`/add-tech-stack`**: Customize an existing plugin stack skill for the current project, or add a brand-new stack (Python, Go, Rust, etc.). Skill: `skills/add-tech-stack/SKILL.md`. Two scenarios:
        - **Scenario A — existing stack**: registered already (default 5 or previously added). Walks each section with `[keep / override / append / skip]`. User picks file scope: SKILL only / AGENT only / both.
        - **Scenario B — new stack**: not registered. Forces both SKILL.md + AGENT.md. Walks template sections with `[input / suggest / skip]`. Best-practice MINIMUM filled on explicit skip.
        - Snapshot stored at `.claude/stack-skills/<stack>/SKILL.md` and `.claude/agents/implementer-<stack>.md`. Project snapshot wins over plugin default at runtime.

      - **`/sync-stack-skill`**: Three-way merge a customized snapshot with the latest plugin default. Skill: `skills/sync-stack-skill/SKILL.md`. Per-section conflict detection (4 patterns); manual-merge 3-pane sub-flow. Supports `--all` for batch sync.

      - **Architecture detection (T1)** integrates into `/init-project` brownfield flow — opt-in scan of project structure to populate the customized stack skill `## Architecture` section. E3 conservative — every step requires explicit user consent.

      Both skills follow the 11-step workflow and use `.claude/stack-skills/registry.json` as the runtime source of truth. Schema reference: `docs/specs/2026-05-07-stack-customization-v5.5.0-design.md`.
      ```

      AVOID:
      - Don't replace the v5.4.0 Bootstrap Commands section — add v5.5.0 section after it.
      - Don't duplicate spec content — high-level pointers only.
      - Don't add to plugin-level template (Plan 02 owned the template change).
      - Don't change BRANCHING RULE / Pull Request Requirements / other unrelated sections.

      Corresponds to: documenting v5.5.0 in primary contributor reference.
    </action>

    <verify>
      <automated>powershell -Command "$c = Get-Content e:/firstwordflow/CLAUDE.md -Raw; $checks = @('## Stack Customization Commands (New in v5.5.0)', '/add-tech-stack', '/sync-stack-skill', 'Scenario A', 'Scenario B', '.claude/stack-skills/registry.json', 'Architecture detection (T1)', '## Bootstrap Commands (New in v5.4.0)'); $missing = $checks | Where-Object { $c -notmatch [regex]::Escape($_) }; if ($missing.Count -eq 0) { Write-Output 'PASS' } else { Write-Output ('FAIL missing: ' + ($missing -join ',')); exit 1 }"</automated>
      <!-- Verifies new v5.5.0 section + key features documented + v5.4.0 section preserved. <60s. -->
    </verify>

    <done>
      Project-root `CLAUDE.md` modified. Has new section `## Stack Customization Commands (New in v5.5.0)` AFTER `## Bootstrap Commands (New in v5.4.0)`. New section mentions `/add-tech-stack` (with Scenario A and B), `/sync-stack-skill`, T1 architecture detection, and references `.claude/stack-skills/registry.json`. v5.4.0 Bootstrap section unchanged.
    </done>

    <stack>plugin-meta</stack>
  </task>

  <task type="auto">
    <name>Update docs/claude/stack-skill-rule-map.md (registry runtime lookup + T2 unknown stack behavior)</name>

    <model>sonnet</model>

    <read_first>
      docs/claude/stack-skill-rule-map.md
      docs/specs/2026-05-07-stack-customization-v5.5.0-design.md
    </read_first>

    <files>
      <modify>docs/claude/stack-skill-rule-map.md</modify>
    </files>

    <action>
      Modify `docs/claude/stack-skill-rule-map.md` to document the v5.5.0 runtime semantics. Two changes:

      **Change 1 — Update "Stack to Mandatory Skill Mapping" section.** After the existing 5-row mapping list, INSERT a new paragraph explaining the runtime lookup order:

      ```markdown
      ## Runtime Lookup Order (v5.5.0+)

      When the agent needs the skill or agent for a stack:

      1. Read `.claude/stack-skills/registry.json` (project-scoped registry, source of truth).
      2. Find the entry where `name == <stack>`:
         - If found and `skill_path` begins with `.claude/...` → load the project snapshot (overrides plugin default).
         - If found and `source == "plugin"` and the path lives in the plugin install dir → load the plugin default listed above.
         - If not found → halt and instruct the user to run `/add-tech-stack <stack>` (T2 trigger).

      Project snapshots always win over plugin defaults when both exist. The 5 plugin defaults remain available as fallbacks until the user customizes them.
      ```

      **Change 2 — Add "Unknown Stack Behavior (T2)" section** at the end of the file, after "How to Apply in Mode B (Team Spine)":

      ```markdown
      ## Unknown Stack Behavior (T2)

      If the agent receives a task that mentions a stack not present in `.claude/stack-skills/registry.json`, the agent MUST halt and present these two options:

      ```
      ⚠ Stack '<name>' not registered in this project's registry.json.
      I cannot proceed without a stack skill.
      💡 Suggestions:
        [1] Run /add-tech-stack <name>  (interactive)
        [2] Cancel task
      ```

      The agent does **not** auto-create the stack. The user must explicitly invoke `/add-tech-stack` (E3 conservative — see spec §7.5).

      Detection heuristic: scan the task text for known framework keywords (FastAPI, Fiber, Axum, Spring, etc.) and for file globs from `detection_markers` of registered stacks. False positives prefer asking over over-triggering.
      ```

      AVOID:
      - Don't remove the existing "Stack to Mandatory Skill Mapping" content — both new sections are additive.
      - Don't duplicate full T2 logic from the spec — keep this map concise; spec is authoritative.
      - Don't change the "Enforcement Rule" section — runtime behavior preserved (every implementation task loads relevant skill).
      - Don't introduce a `--use-web` flag here — opt-in per prompt is enforced in skills, not the rule-map.

      Corresponds to REQ-IDs: STACK-05 (T2 unknown stack behavior), STACK-07 (snapshot model — runtime lookup order).
    </action>

    <verify>
      <automated>powershell -Command "$c = Get-Content e:/firstwordflow/docs/claude/stack-skill-rule-map.md -Raw; $checks = @('Runtime Lookup Order (v5.5.0+)', '.claude/stack-skills/registry.json', 'Unknown Stack Behavior (T2)', '/add-tech-stack', 'Project snapshots always win', 'Enforcement Rule', 'Stack to Mandatory Skill Mapping'); $missing = $checks | Where-Object { $c -notmatch [regex]::Escape($_) }; if ($missing.Count -eq 0) { Write-Output 'PASS' } else { Write-Output ('FAIL missing: ' + ($missing -join ',')); exit 1 }"</automated>
      <!-- Verifies both new sections present + registry reference + existing sections preserved. <60s. -->
    </verify>

    <done>
      File `docs/claude/stack-skill-rule-map.md` modified. Has new `## Runtime Lookup Order (v5.5.0+)` section describing 3-step lookup with `.claude/stack-skills/registry.json` as source of truth. Has new `## Unknown Stack Behavior (T2)` section documenting halt + 2-option flow with no auto-create. Existing sections (Enforcement Rule, Stack to Mandatory Skill Mapping, How to Apply Mode A, How to Apply Mode B) preserved.
    </done>

    <stack>plugin-meta</stack>
  </task>

</wave>

<!--
  PLAN CHECKER mental review:
  1. Requirement Coverage — STACK-05 (T2 behavior), STACK-07 (runtime lookup), STACK-12 (release artifacts) covered. ✓
  2. Task Completeness — model + read_first + action + verify + done all present. ✓
  3. Dependency Correctness — depends_on [01, 02, 03]. T10 modifies 4 release files (no overlap with T11/T12). T11 modifies CLAUDE.md (project root). T12 modifies stack-skill-rule-map.md. No file overlap. ✓
  4. Key Links Planned — version 5.5.0 across 3 manifests + CHANGELOG; CLAUDE.md v5.5.0 section parallel to v5.4.0; registry referenced from rule-map. ✓
  5. Scope Sanity — 3 tasks/plan. ✓
  6. Verification Derivation — must_haves trace via verify regex. ✓
  7. Context Compliance — N/A. ✓
  8. Nyquist Compliance — 3 verify commands <60s. ✓
  9. Cross-Plan Data Contracts — Plan 04 closes the loop: CLAUDE.md + rule-map reference registry from Plan 01, document skills from Plan 02, document T1 detection from Plan 03. ✓
  10. CLAUDE.md Compliance — XML format. ✓
  11. Research Resolution — N/A. ✓
-->