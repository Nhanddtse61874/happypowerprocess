---
phase: v5.5.0-stack-customization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/claude/templates/registry.json
  - docs/claude/templates/stack-skill-template.md
  - docs/claude/templates/stack-agent-template.md
autonomous: true
requirements: [STACK-06, STACK-11]

must_haves:
  truths:
    - "registry.json template parses as valid JSON, schema version '1', 5 plugin default stacks"
    - "stack-skill-template.md has 6 fixed sections (Required Rules / Quality Gates / Output / Architecture / Anti-Patterns / Verification Matrix) + variable placeholder"
    - "stack-agent-template.md uses inherit model and contains all 9 placeholders defined in spec §6.2"
  artifacts:
    - "docs/claude/templates/registry.json"
    - "docs/claude/templates/stack-skill-template.md"
    - "docs/claude/templates/stack-agent-template.md"
  key_links:
    - "registry.json schema matches spec §5.5 (10 fields per stack)"
    - "stack-skill-template placeholders match spec §6.1 placeholder semantics table"
    - "stack-agent-template placeholders match spec §6.2 placeholder table"
---

<!--
  WAVE 1 — Foundation templates. All 3 tasks independent, no file overlap, run in parallel.
  Spec reference: docs/specs/2026-05-07-stack-customization-v5.5.0-design.md
-->

<wave number="1" description="Foundation templates — registry + skill template + agent template (parallel)">

  <task type="auto">
    <name>Create docs/claude/templates/registry.json with 5 plugin default stacks</name>

    <model>haiku</model>

    <read_first>
      docs/specs/2026-05-07-stack-customization-v5.5.0-design.md
      docs/claude/stack-skill-rule-map.md
    </read_first>

    <files>
      <create>docs/claude/templates/registry.json</create>
    </files>

    <action>
      Create `docs/claude/templates/registry.json` matching spec §6.3 EXACTLY. Content:

      ```json
      {
        "version": "1",
        "stacks": [
          {
            "name": "dotnet",
            "label": ".NET / C#",
            "description": "ASP.NET Core, EF Core, async backend",
            "skill_path": "skills/implementer-dotnet-csharp/SKILL.md",
            "agent_path": "agents/implementer-dotnet-csharp.md",
            "detection_markers": ["*.csproj", "*.sln"],
            "source": "plugin",
            "synced_from_plugin_version": "{PLUGIN_VERSION}",
            "last_synced": "{INIT_DATE}"
          },
          {
            "name": "react",
            "label": "React / TypeScript",
            "description": "React web with hooks, TanStack Query, Zustand",
            "skill_path": "skills/implementer-react-typescript/SKILL.md",
            "agent_path": "agents/implementer-react-typescript.md",
            "detection_markers": ["package.json"],
            "source": "plugin",
            "synced_from_plugin_version": "{PLUGIN_VERSION}",
            "last_synced": "{INIT_DATE}"
          },
          {
            "name": "react-native",
            "label": "React Native / Expo",
            "description": "Mobile with React Navigation, MMKV, TanStack Query",
            "skill_path": "skills/implementer-react-native-typescript/SKILL.md",
            "agent_path": "agents/implementer-react-native-typescript.md",
            "detection_markers": ["app.json", "metro.config.js"],
            "source": "plugin",
            "synced_from_plugin_version": "{PLUGIN_VERSION}",
            "last_synced": "{INIT_DATE}"
          },
          {
            "name": "angular",
            "label": "Angular / TypeScript",
            "description": "Angular 17+ with Signals, RxJS, NgModule or standalone",
            "skill_path": "skills/implementer-angular-typescript/SKILL.md",
            "agent_path": "agents/implementer-angular-typescript.md",
            "detection_markers": ["angular.json"],
            "source": "plugin",
            "synced_from_plugin_version": "{PLUGIN_VERSION}",
            "last_synced": "{INIT_DATE}"
          },
          {
            "name": "iot-edge",
            "label": "IoT / MQTT / BLE",
            "description": "Edge device connectivity with MQTT/BLE/firmware",
            "skill_path": "skills/implementer-iot-edge/SKILL.md",
            "agent_path": "agents/implementer-iot-edge.md",
            "detection_markers": [],
            "source": "plugin",
            "synced_from_plugin_version": "{PLUGIN_VERSION}",
            "last_synced": "{INIT_DATE}"
          }
        ]
      }
      ```

      `{PLUGIN_VERSION}` and `{INIT_DATE}` are placeholder strings — kept verbatim in template. `/init-project` substitutes them at runtime when copying template to `.claude/stack-skills/registry.json`.

      AVOID:
      - Don't add a 6th stack (out of scope — user adds via /add-tech-stack).
      - Don't change `source: plugin` (state machine spec §5.4 — user-customized snapshots flip to "customized" only at runtime, not in template).
      - Don't omit `detection_markers` for `iot-edge` — empty array `[]` is intentional (spec §6.3 note).
      - Don't replace `{PLUGIN_VERSION}` / `{INIT_DATE}` with literal values — those are runtime placeholders.

      Corresponds to REQ-ID: STACK-06 (registry schema + plugin defaults).
    </action>

    <verify>
      <automated>powershell -Command "try { $r = Get-Content e:/firstwordflow/docs/claude/templates/registry.json -Raw | ConvertFrom-Json; if ($r.version -eq '1' -and $r.stacks.Count -eq 5 -and ($r.stacks | Where-Object { $_.name -eq 'dotnet' }).Count -eq 1 -and ($r.stacks | Where-Object { $_.source -eq 'plugin' }).Count -eq 5) { Write-Output 'PASS' } else { Write-Output 'FAIL: structure mismatch'; exit 1 } } catch { Write-Output 'FAIL: invalid JSON'; exit 1 }"</automated>
      <!-- Verifies valid JSON, version=1, 5 stacks, all source=plugin, dotnet present. <60s. -->
    </verify>

    <done>
      File `docs/claude/templates/registry.json` exists. Parses as valid JSON. Has `version: "1"`. `stacks` array contains exactly 5 entries with names: dotnet, react, react-native, angular, iot-edge. All 5 have `source: "plugin"` and `synced_from_plugin_version: "{PLUGIN_VERSION}"`. `iot-edge.detection_markers` is `[]`.
    </done>

    <stack>plugin-meta</stack>
  </task>

  <task type="auto">
    <name>Create docs/claude/templates/stack-skill-template.md (template for new stack SKILL.md)</name>

    <model>haiku</model>

    <read_first>
      docs/specs/2026-05-07-stack-customization-v5.5.0-design.md
      skills/implementer-dotnet-csharp/SKILL.md
    </read_first>

    <files>
      <create>docs/claude/templates/stack-skill-template.md</create>
    </files>

    <action>
      Create `docs/claude/templates/stack-skill-template.md` matching spec §6.1 EXACTLY. Content:

      ```markdown
      ---
      name: implementer-{STACK_NAME}
      description: {DESCRIPTION}
      ---

      # Implementer {STACK_LABEL}

      Apply this skill for {STACK_LABEL} implementation tasks.

      ## Required Rules

      {REQUIRED_RULES}

      ## Minimum Quality Gates

      {QUALITY_GATES}

      ## Output Expectations

      - Changed files with rationale
      - Test and verification evidence
      - Risk and follow-up items

      ---

      ## Architecture

      {ARCHITECTURE}

      ---

      {VARIABLE_SECTIONS}

      ---

      ## Anti-Patterns

      {ANTI_PATTERNS}

      ---

      ## Verification Matrix

      {VERIFICATION_MATRIX}
      ```

      The template uses 9 placeholders: `{STACK_NAME}`, `{STACK_LABEL}`, `{DESCRIPTION}`, `{REQUIRED_RULES}`, `{QUALITY_GATES}`, `{ARCHITECTURE}`, `{VARIABLE_SECTIONS}`, `{ANTI_PATTERNS}`, `{VERIFICATION_MATRIX}`. The 3-bullet `## Output Expectations` content is hardcoded (consistent across all 5 plugin defaults — spec §6.1 placeholder table notes this is not a placeholder).

      AVOID:
      - Don't add stack-specific content (this is a generic template).
      - Don't omit any of the 9 placeholders.
      - Don't change section order — fixed top (Required/Quality/Output) → Architecture → variable → fixed bottom (Anti-Patterns/Verification) per spec §6.1 structure.
      - Don't change frontmatter keys — `name` and `description` only (no `model`, `tags`, etc.).

      Corresponds to REQ-ID: STACK-11 (best-practice MINIMUM templates).
    </action>

    <verify>
      <automated>powershell -Command "$c = Get-Content e:/firstwordflow/docs/claude/templates/stack-skill-template.md -Raw; $required = @('{STACK_NAME}', '{STACK_LABEL}', '{DESCRIPTION}', '{REQUIRED_RULES}', '{QUALITY_GATES}', '{ARCHITECTURE}', '{VARIABLE_SECTIONS}', '{ANTI_PATTERNS}', '{VERIFICATION_MATRIX}', '## Required Rules', '## Architecture', '## Anti-Patterns', '## Verification Matrix'); $missing = $required | Where-Object { $c -notmatch [regex]::Escape($_) }; if ($missing.Count -eq 0) { Write-Output 'PASS' } else { Write-Output ('FAIL missing: ' + ($missing -join ',')); exit 1 }"</automated>
      <!-- Verifies all 9 placeholders + 4 section headers present. <60s. -->
    </verify>

    <done>
      File `docs/claude/templates/stack-skill-template.md` exists. Contains all 9 placeholders: `{STACK_NAME}`, `{STACK_LABEL}`, `{DESCRIPTION}`, `{REQUIRED_RULES}`, `{QUALITY_GATES}`, `{ARCHITECTURE}`, `{VARIABLE_SECTIONS}`, `{ANTI_PATTERNS}`, `{VERIFICATION_MATRIX}`. Has all 6 fixed section headers (`## Required Rules`, `## Minimum Quality Gates`, `## Output Expectations`, `## Architecture`, `## Anti-Patterns`, `## Verification Matrix`). Frontmatter has only `name` and `description` keys.
    </done>

    <stack>plugin-meta</stack>
  </task>

  <task type="auto">
    <name>Create docs/claude/templates/stack-agent-template.md (template for new stack AGENT.md)</name>

    <model>haiku</model>

    <read_first>
      docs/specs/2026-05-07-stack-customization-v5.5.0-design.md
      agents/implementer-dotnet-csharp.md
    </read_first>

    <files>
      <create>docs/claude/templates/stack-agent-template.md</create>
    </files>

    <action>
      Create `docs/claude/templates/stack-agent-template.md` matching spec §6.2 EXACTLY. Content:

      ```markdown
      ---
      name: implementer-{STACK_NAME}
      description: {AGENT_DESCRIPTION}
      model: inherit
      ---

      You implement {TASK_DOMAIN} tasks in {STACK_LABEL}. {USER_GOAL}

      **REQUIRED:** Read and apply `{SKILL_PATH}` before writing any code. That skill is the authoritative guide for {KEY_TOPICS}.

      Execution rules:
      {EXECUTION_RULES}

      Output contract:
      - Changed files and rationale
      - Tests added/updated and results
      - Risk notes and operational considerations
      ```

      The template uses 8 placeholders: `{STACK_NAME}`, `{AGENT_DESCRIPTION}`, `{TASK_DOMAIN}`, `{STACK_LABEL}`, `{USER_GOAL}`, `{SKILL_PATH}`, `{KEY_TOPICS}`, `{EXECUTION_RULES}`. The 3-bullet "Output contract" is hardcoded (consistent across all 5 plugin default agents).

      AVOID:
      - Don't add `tags`, `version`, or other frontmatter keys — only `name`, `description`, `model: inherit`.
      - Don't omit any of the 8 placeholders.
      - Don't change `model: inherit` — all plugin default agents use `inherit` (spec §12.1 branding note).
      - Don't add execution rules content — `{EXECUTION_RULES}` is filled by user input at /add-tech-stack runtime.

      Corresponds to REQ-ID: STACK-11 (best-practice MINIMUM templates — agent side).
    </action>

    <verify>
      <automated>powershell -Command "$c = Get-Content e:/firstwordflow/docs/claude/templates/stack-agent-template.md -Raw; $required = @('{STACK_NAME}', '{AGENT_DESCRIPTION}', '{TASK_DOMAIN}', '{STACK_LABEL}', '{USER_GOAL}', '{SKILL_PATH}', '{KEY_TOPICS}', '{EXECUTION_RULES}', 'model: inherit', 'Output contract:'); $missing = $required | Where-Object { $c -notmatch [regex]::Escape($_) }; if ($missing.Count -eq 0) { Write-Output 'PASS' } else { Write-Output ('FAIL missing: ' + ($missing -join ',')); exit 1 }"</automated>
      <!-- Verifies all 8 placeholders + frontmatter `model: inherit` + Output contract section. <60s. -->
    </verify>

    <done>
      File `docs/claude/templates/stack-agent-template.md` exists. Contains all 8 placeholders: `{STACK_NAME}`, `{AGENT_DESCRIPTION}`, `{TASK_DOMAIN}`, `{STACK_LABEL}`, `{USER_GOAL}`, `{SKILL_PATH}`, `{KEY_TOPICS}`, `{EXECUTION_RULES}`. Frontmatter has `model: inherit`. Body has REQUIRED skill reference + Execution rules + Output contract sections.
    </done>

    <stack>plugin-meta</stack>
  </task>

</wave>

<!--
  PLAN CHECKER mental review:
  1. Requirement Coverage — STACK-06 (registry), STACK-11 (templates) covered. ✓
  2. Task Completeness — model + read_first + action + verify + done all present. ✓
  3. Dependency Correctness — Wave 1, no deps, acyclic. ✓
  4. Key Links Planned — placeholder coverage matches spec tables. ✓
  5. Scope Sanity — 3 tasks/plan, max per CLAUDE.md. ✓
  6. Verification Derivation — must_haves trace to phase truths. ✓
  7. Context Compliance — N/A (no CONTEXT.md for plugin self-dev). ✓
  8. Nyquist Compliance — 3 PowerShell verify commands all <60s. ✓
  9. Cross-Plan Data Contracts — registry.json template consumed by Plan 03 (init-project update). stack-*-template consumed by Plan 02 (skills) + Plan 03. ✓
  10. CLAUDE.md Compliance — XML format with model/read_first/action/verify/done. ✓
  11. Research Resolution — N/A (research skipped per user). ✓
-->
